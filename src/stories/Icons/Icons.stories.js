import { iconsList } from './Icons';

export default {
  title: 'Components/Icons',
  tags: ['autodocs'],
  render: (args) => iconsList(args),
  argTypes: {
  },
  args: {
  },
};

export const Icons = {};